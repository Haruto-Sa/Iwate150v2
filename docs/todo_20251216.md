##岩手県観光アプリ作成TODO(20251216)

#全体
-背景が暗めになっているので、ユーザーが使いやすい程度の明るさに変更して。文字についても、黒ベースで、背景が黒と色が被って文字が見えないという問題が発生しないような色を選択して。skills.mdのデザインについては意識した上で変更を行って。

#カメラ機能
-モデル表示を顔認識→モデル表示をUXを意識したものにして。
-カメラページが出たらすぐにカメラ起動(ユーザ側認証はあるものとして)が行われるようにして。今はボタン押さないとカメラが起動しない
-カメラボタンが変な位置に小さいサイズで置いてある。半透明かつ大きめにおいて。
-モデル表示が現在カメラの中心にそばっちが固定で配置され顔が見えるとモデルが顔の上側に配置され回転し続ける
 -p5,m5のfacemeshの機能を使って顔を認識して顔に沿って(顔に重ならないように顔の周りに)モデルが移動(選択したモデル)するようにして
 -5種類どれかしか選べないので複数選択できるようにして。数によって顔の周りに配置するモデルの配置位置を調整して。(1→顔の上部、2→顔の左右,3→上、右下、左下、4→左右斜め上下、5→顔を中心とした正五角形の頂点)
-シャッターを押すとモデルが表示されていない写真ができてしまうのでモデルも一緒に映るように修正して

#編集機能
-カメラから来た写真に対して、左上に絵文字が2つ、左下に文字が並んでいる(編集不可)。位置が悪いしダサいので、顔とモデルに被らない位置に適当な絵文字を配置するようにして
-文字に関しては、フォントを選んで位置を自由に決められるようにして。(どちらもinstagramのリール作る時みたいに自由に決められるようにしたい)
-画像を端末に保存する機能が未実装なので、これを実装して。legacyではファイルに保存するedit.jsがあるのでもしweb等で技術がどうしても探せない場合は参考にして。ただ処理速度が遅いなら最も早く質を担保する技術を使うようにして。(スマホなら画像として写真に保存、PCならファイルとして保存)

#map機能
-現在位置の近く10個観光地を出すようにして。検出方法は直線距離が近い順。また、検出数を増やせるようにして。(最大30個)
-現在地を更新で位置情報を更新できるようにして。ユーザーのデータ通信に支障が出ない範囲でアプリ利用時(map使用時)は位置情報を取得して現在地を更新するようにして。
-半径500mのスポットの位置測定がおかしいので修正して。

#character機能
-モデル表示が全くされていないので、これが正しく動作するように修正して。
-ゆるキャラの紹介が途中で途切れているので、各キャラクターのタブをタップしたら大きめの詳細が見れるようにして。
-モデルについて、
    /Users/harutosasaki/GithubRepo/Iwate150v2/legacy/flask_app/templates/stamp.html,
    /Users/harutosasaki/GithubRepo/Iwate150v2/legacy/flask_app/static/css/main.css,
    /Users/harutosasaki/GithubRepo/Iwate150v2/legacy/flask_app/static/javascript/stamp.js
    これらが元のモデル配置になっている。現在は背景が暗いので、モデルがよく見えるように、元のページのように明るめのグレーもしくは白背景にしてモデルがよく見えるようにして。
